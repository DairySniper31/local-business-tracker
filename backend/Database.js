const fs = require('fs');

class Database {

    constructor(userFile) {
        fs.readFile(userFile, (err, data) => {
            if (err) throw err;

            const
        })
    }
}

module.exports = Database